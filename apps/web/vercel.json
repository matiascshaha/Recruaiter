{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@9.6.0 --activate && cd ../.. && pnpm install -w",
  "buildCommand": "corepack enable && corepack prepare pnpm@9.6.0 --activate && cd ../.. && pnpm --filter web build"
}
